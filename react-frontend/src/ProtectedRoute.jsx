/**
 * ProtectedRoute.jsx (root-level re-export)
 *
 * The real implementation lives in components/ProtectedRoute.jsx.
 * This file simply re-exports it so that App.jsx can import from
 * either location without breaking.
 */
export { default } from './components/ProtectedRoute';
