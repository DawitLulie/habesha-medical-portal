/**
 * Router.jsx
 *
 * Thin wrapper that re-exports the full application.
 * All routing logic lives inside App.jsx (AppRoutes component).
 * Import App directly for the entry point; this file exists for
 * any tooling or tests that expect a dedicated Router module.
 */
export { default } from './App';
